<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - EvaluaAI</title>
    <style>
        /* styles.css */

body {
    background-color: #000; /* Black background */
    color: #fff; /* White text */
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.container {
    text-align: center;
    background-color: #1a1a1a; /* Dark grey for the container */
    padding: 40px;
    border-radius: 10px;
    box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.5);
    width: 350px;
}

h1 {
    font-size: 2rem;
    margin-bottom: 20px;
    color: #e0e0e0; /* Light grey for heading */
}

form {
    display: flex;
    flex-direction: column;
}

input[type="text"],
input[type="password"],
select {
    background-color: #333; /* Dark input background */
    color: #fff;
    border: 1px solid #666; /* Light border */
    padding: 10px;
    margin: 10px 0;
    border-radius: 5px;
    font-size: 1rem;
}

input[type="text"]:focus,
input[type="password"]:focus,
select:focus {
    border-color: #ff9900; /* Orange highlight on focus */
    outline: none;
}

button {
    background-color: #ff9900; /* Orange for the button */
    color: #000;
    padding: 10px 20px;
    margin: 20px 0;
    text-decoration: none;
    border-radius: 5px;
    font-size: 1rem;
    font-weight: bold;
    cursor: pointer;
    transition: background-color 0.3s ease;
    border: none;
}

button:hover {
    background-color: #ff6600; /* Darker orange on hover */
}

button:active {
    background-color: #e65c00; /* Even darker orange on click */
}

p {
    margin: 20px 0 0;
    font-size: 0.9rem;
    color: #bbb;
}

a {
    color: #ff9900; /* Orange for links */
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}

.error {
    color: #ff3333; /* Red for error messages */
    font-size: 0.9rem;
    margin-bottom: 15px;
}

    </style>
    <!-- <link rel="stylesheet" href="/css/styles.css"> -->
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <% if (locals.error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <select name="role" required>
                <option value="teacher">Teacher</option>
                <option value="student">Student</option>
            </select>
            <button type="submit">Login</button>
        </form>
        <p>Don't have an account? <a href="/signup">Sign up</a></p>
    </div>
</body>
</html>